CREATE TABLE admins
(
    id    BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    email VARCHAR(255)                            NOT NULL,
    CONSTRAINT pk_admins PRIMARY KEY (id)
);

ALTER TABLE admins
    ADD CONSTRAINT FK_ADMINS_ON_EMAIL FOREIGN KEY (email) REFERENCES users (email);
